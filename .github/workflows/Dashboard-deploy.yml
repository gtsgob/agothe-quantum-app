name: Deploy dashboard to gh-pages

# Deploys /dashboard/ to the gh-pages branch so GitHub Pages can serve it
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Ensure gh-pages branch and deploy dashboard
        env:
          REPO: ${{ github.repository }}
          SOURCE_DIR: dashboard
        run: |
          set -e

          cd "$GITHUB_WORKSPACE"

          # Verify dashboard exists
          if [ ! -d "$SOURCE_DIR" ]; then
            echo "❌ Directory '$SOURCE_DIR' not found in repo. Aborting."
            exit 1
          fi

          # Copy dashboard to temp location BEFORE switching branches
          TEMP_DASHBOARD=$(mktemp -d)
          cp -a "$SOURCE_DIR/." "$TEMP_DASHBOARD/"

          # Fetch existing gh-pages if it exists
          git fetch origin gh-pages || true

          # Switch to gh-pages branch, create if missing
          if git show-ref --verify --quiet refs/remotes/origin/gh-pages; then
            git switch gh-pages
          else
            git switch --orphan gh-pages
            git rm -rf . || true
          fi

          # Clean branch workspace
          git rm -rf . || true

          # Copy dashboard from temp location
          mkdir -p dashboard
          cp -a "$TEMP_DASHBOARD/." dashboard/
          rm -rf "$TEMP_DASHBOARD"

          # Commit and push
          git add -A
          if git diff --cached --quiet; then
            echo "⚙️ No changes to deploy."
          else
            git commit -m "🚀 Deploy dashboard to gh-pages (from $GITHUB_REF)"
            git push origin gh-pages --force --quiet
            echo "✅ Deployment complete!"
          fi

      - name: Post deployment summary
        if: always()
        run: |
          echo "🌐 Dashboard live at:"
          echo "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/dashboard/"
